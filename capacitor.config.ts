import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aliasist.filesabductor',
  appName: 'Aliasist Files Abductor',
  webDir: 'dist'
};

export default config;
